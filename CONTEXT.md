# JasoWatch

Windows 폴더 안에서 분해된 Unicode 파일명을 감지하고 정상화하는 포터블 유틸리티의 도메인 언어다.

## Language

**감시 폴더**:
JasoWatch가 새 항목과 이름 변경을 관찰하여 파일명 정상화 대상으로 삼는 사용자가 지정한 폴더.
_Avoid_: 대상 폴더, 다운로드 폴더

**감시 일시 중단**:
사용자가 명시적으로 요청했거나 감시 폴더에 접근할 수 없어 파일명 정규화 감시를 수행하지 않는 상태.
_Avoid_: 비활성화, 종료

**감시 범위**:
감시 폴더와, 사용자가 선택한 경우 그 실제 하위 폴더로 이루어진 파일명 정상화의 대상 영역. 재분석 지점이 가리키는 위치는 범위에 포함하지 않는다.
_Avoid_: 재귀 폴더, 링크 대상

**분해된 파일명**:
파일명에 조합 가능한 Unicode 표준 자모가 분해된 형태로 들어 있어 NFC 정규화 결과가 원래 이름과 다른 항목.
_Avoid_: 깨진 파일명, 호환 자모 파일명

**정상화된 파일명**:
분해된 파일명을 Unicode NFC로 변환해 얻는, 기존 항목을 덮어쓰지 않는 최종 파일명.
_Avoid_: 수정된 파일명, 복구된 파일명

**정상화 대기**:
이벤트를 감지한 항목이 아직 내려받기 또는 쓰기 중일 수 있어, 안정성 및 파일 잠금 검사를 통과할 때까지 파일명 변경을 미루는 상태.
_Avoid_: 다운로드 완료, 지연 처리

**정상화 실패**:
정상화 대기 중인 항목이 정해진 재시도 기간 안에 이름 변경을 완료하지 못한 상태. 다른 항목의 감시는 계속된다.
_Avoid_: 감시 실패, 앱 오류

**기존 파일명 정리**:
사용자가 시작하는 일회성 정상화 작업으로, 현재 감시 범위에 이미 있는 항목을 검사한다.
_Avoid_: 초기 동기화, 전체 정리

**실행 인스턴스**:
트레이 아이콘과 감시 작업을 소유하는 JasoWatch 프로세스. 사용자 세션마다 하나만 존재할 수 있다.
_Avoid_: 앱 창, 감시 프로세스

**자동 시작**:
Windows 로그인 뒤 현재 사용자의 JasoWatch 실행 인스턴스를 자동으로 시작하는 사용자가 선택하는 설정.
_Avoid_: 시스템 서비스, 설치

**이벤트 복구 검사**:
파일 시스템 이벤트가 유실된 뒤 감시 범위를 다시 검사해 놓친 정상화 대상을 찾는 자동 복구 작업.
_Avoid_: 기존 파일명 정리, 정기 검사

**상태 알림**:
사용자의 확인이 필요한 JasoWatch 상태 변화를 트레이를 통해 한 번 알리는 메시지. 개별 파일명 정상화에는 표시하지 않는다.
_Avoid_: 변경 알림, 작업 로그

**임시 항목**:
다운로드 진행 또는 다른 프로그램의 작업 상태를 나타내므로 파일명 정상화 대상에서 제외하는 파일. 기본 규칙은 지정된 확장자와 `~$` 파일명 접두사를 사용한다.
_Avoid_: 정상화 실패 항목, 숨김 파일
