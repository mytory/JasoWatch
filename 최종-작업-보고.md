# JasoWatch 첫 번째 동작 버전 작업 보고

## 완료 내역

- .NET 10 WinForms 기반의 포터블 Windows 트레이 앱을 구성했습니다.
- 새 파일/이름 변경을 감시하고, 분해된 Unicode 파일명을 NFC로 안전하게 변경합니다.
- 이름 충돌 시 가장 작은 번호를 붙이고, 파일별 안정성·잠금 검사를 거친 뒤 재시도합니다.
- 하위 폴더, 임시 파일 제외 규칙, 기존 파일명 정리, 감시 일시 중단, 이벤트 복구 검사, 설정, Windows 자동 시작, 단일 인스턴스와 상태 아이콘을 구현했습니다.
- `JasoWatch.Tests`에 핵심 규칙과 실제 임시 파일 이름 변경을 검증하는 테스트 9개를 작성했고 모두 통과했습니다.
- 배포 실행 파일은 `release/JasoWatch-v1/JasoWatch.exe`에 생성했으며, 단일 EXE ZIP은 `release/JasoWatch-win-x64.zip`입니다.

## 명세 해석 및 판단

- `Changed` 이벤트는 정상화 실패 항목만 재시도 신호로 쓰라는 요구와, 쓰기 중 파일의 안정성 확인 필요성 사이에 약간의 충돌이 있습니다. 첫 버전에서는 `Changed`를 새 항목의 debounce 갱신에도 사용했습니다. 파일 작성 중 변경 이벤트가 계속 오면 정상화가 미뤄져 안전하며, 이미 NFC인 항목은 즉시 무시됩니다.
- 재분석 지점은 하위 항목을 탐색하지 않고 링크 자체만 정상화 대상으로 포함했습니다.
- 감시 폴더 접근 불가 상태의 복구 확인은 5초 주기로 수행합니다. 명세가 주기를 정하지 않아 사용자 경험과 부하의 균형을 위해 선택했습니다.
- Windows 파일 시스템의 실제 비교 규칙은 `File.Exists`/`Directory.Exists`에 위임했습니다. 일반적인 대소문자 구분 없는 NTFS 동작을 그대로 따릅니다.

## 검증

- `dotnet test JasoWatch.Tests/JasoWatch.Tests.csproj -c Release`: 통과 9, 실패 0
- `dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true`: 성공
